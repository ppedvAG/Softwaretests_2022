<UserControl x:Class="ppedv.Autovermietung.UI.WPF.View.AutoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ppedv.Autovermietung.UI.WPF.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <DockPanel>
        <WrapPanel DockPanel.Dock="Top">
            <Button>Laden</Button>
            <Button>Speichern</Button>
            <Button>Neu</Button>
        </WrapPanel>
        <DataGrid DockPanel.Dock="Left" Width="200"/>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Label>Hersteller</Label>
            <TextBox Grid.Column="1"/>
            <Label Grid.Row="1">Modell</Label>
            <TextBox Grid.Row="1" Grid.Column="1"/>
            <Label Grid.Row="2">Baujahr</Label>
            <DatePicker Grid.Row="2" Grid.Column="1"/>
            <Label Grid.Row="3">PS</Label>
            <Slider Grid.Row="3" Grid.Column="1" Maximum="1000"/>
            <GroupBox Header="Vermietungen" Grid.Row="4" Grid.ColumnSpan="2">
                <ListBox/>
            </GroupBox>
        </Grid>
    </DockPanel>
</UserControl>
